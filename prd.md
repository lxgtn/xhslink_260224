# PRD：XHS Link 小红书内容聚合工具

**版本**：v1.0
**日期**：2026-02-24
**状态**：草稿

---

## 1. 产品概述

### 1.1 背景与问题

内容研究者在分析小红书话题、追踪博主动态或开展竞品研究时，需要手动逐条打开笔记、记录内容要点，效率极低，且无法批量处理大量链接。图片和视频内容更需人工逐一查阅，难以形成可供检索和分析的结构化数据。

### 1.2 产品定位

**XHS Link** 是一款面向个人内容研究者的小红书内容批量采集与智能聚合工具。用户只需在 Google Sheets 中填写小红书链接，点击一个按钮，系统即可自动完成内容抓取、多模态 AI 解析（图片/视频）和内容总结，并将结构化结果回写至原始表格，形成可直接使用的研究数据。

### 1.3 核心价值

- **批量处理**：一次性处理多条链接，无需逐条手动操作
- **多模态解析**：自动解析笔记中的图片文字和视频内容，不遗漏关键信息
- **结构化输出**：所有数据以标准化格式回写 Google Sheets，可直接用于后续分析
- **低门槛使用**：无需技术背景，输入链接、点击按钮即可完成全流程

---

## 2. 目标用户

### 2.1 用户画像

| 属性 | 描述 |
|------|------|
| **用户类型** | 个人内容研究者 |
| **典型身份** | 自媒体从业者、品牌研究员、学术研究者、兴趣爱好者 |
| **核心需求** | 批量采集特定话题或博主的小红书内容，生成可分析的结构化数据 |
| **技术水平** | 中等；熟悉 Google Sheets，无需编程能力 |

### 2.2 典型使用场景

- **话题研究**：收集某个关键词下的多篇笔记，快速了解内容分布和主要观点
- **博主追踪**：定期采集目标博主新发笔记，汇总内容动态
- **竞品分析**：批量抓取竞品相关笔记，提取文字和视觉内容形成对比报告
- **灵感收集**：将收藏的小红书链接批量转化为可搜索的文本档案

---

## 3. 功能需求

### F1 数据输入：Google Sheets

**描述**：用户通过指定的 Google Sheets 表格管理待处理的小红书链接列表。

**详细需求**：
- 用户在表格的 `link` 列中填写一条或多条小红书笔记链接
- 系统每次运行时，自动筛选出 `auto` 字段**和** `error` 字段均为空值的行作为本次处理任务
- 已成功处理（`auto=1`）或已标记错误（`error` 非空）的行在下次运行时自动跳过，不重复处理
- 表格结构见第 5 节数据模型

---

### F2 工作流触发

**描述**：用户通过网页端的操作按钮手动触发完整处理工作流。

**详细需求**：
- 提供一个独立网页作为操作控制台
- 网页上包含**「开始运行」**按钮，点击后立即触发工作流
- 工作流运行期间，按钮变为不可点击状态，防止重复触发
- 工作流完成后，按钮恢复可点击状态，并在状态区域展示本次运行结果摘要

---

### F3 网页端用户界面

**描述**：操作控制台提供可视化的运行状态、日志、配置和历史记录功能。

#### F3.1 运行进度与状态展示
- 显示当前处理状态（空闲 / 运行中 / 已完成 / 出错）
- 运行中实时显示进度，例如：`正在处理第 3 / 10 条：[链接标题或链接地址]`
- 完成后显示本次运行汇总：成功 N 条、失败 N 条

#### F3.2 实时运行日志
- 在网页上实时滚动展示每个处理步骤的日志信息
- 日志内容包括：每条链接的处理开始/完成时间、每个步骤（抓取、图片解析、视频解析、总结、回写）的执行状态
- 错误信息以醒目方式标注（如红色文字），并显示具体错误原因

#### F3.3 历史运行记录
- 保存每次运行的历史记录
- 历史记录列表展示：运行时间、处理总条数、成功/失败数
- 支持查看历史运行的详细日志

#### F3.4 AI 模型配置面板
- 提供 AI 模型配置界面，用户可填写以下信息：
  - **模型厂商**（如 OpenAI、Anthropic、Google 等）
  - **模型名称**（如 `gpt-4o`、`claude-3-5-sonnet`、`gemini-2.0-flash`）
  - **API Base URL**（可选，用于自定义接入端点）
  - **API Key**
- 配置信息在本地持久化保存，重启后无需重新填写
- API Key 以密码形式显示（掩码），保护敏感信息

#### F3.5 小红书 Cookie 获取
- 网页端提供小红书账号鉴权入口
- 系统自动检测并获取当前浏览器中已登录小红书账号的 Cookie 信息，用于后续数据抓取的身份验证
- 显示当前 Cookie 状态（已获取 / 未获取 / 已过期）
- 提供手动刷新 Cookie 的操作按钮
- **技术说明**：由于浏览器同源策略限制，Cookie 自动获取功能可能需要通过浏览器扩展插件或本地客户端部署方式实现，具体方案在技术选型阶段确定

---

### F4 小红书数据抓取

**描述**：系统自动访问每条小红书链接，抓取笔记的结构化数据。

**抓取字段**：

| 字段名 | 说明 | 备注 |
|--------|------|------|
| `title` | 笔记标题 | |
| `author` | 作者昵称 | |
| `date` | 发布时间 | |
| `stars` | 收藏量 | |
| `text_original` | 笔记正文文字内容 | |
| `pic_url_list` | 笔记图片地址列表 | 多张图片以列表形式存储 |
| `video_url_list` | 笔记视频地址列表 | 无视频时为空 |

**详细需求**：
- 抓取请求携带 F3.5 获取的小红书账号 Cookie，确保能访问登录后可见的内容
- 单条链接抓取失败时，记录错误原因并继续处理下一条，不中断整体工作流

---

### F5 图片内容解析

**描述**：使用配置的 AI 大模型对笔记中的图片进行多模态解析，提取视觉内容和文字信息。

**详细需求**：
- 对 `pic_url_list` 中的每张图片，依次调用 AI 模型进行解析：
  - **纯视觉图片**（无文字）：用简短文字描述图片中的主要视觉内容
  - **含文字图片**（截图、信息图、带字图等）：提取图片中的完整文字内容
- 多张图片的解析结果按照原始图片顺序排列，以数字序号标注，例如：
  ```
  1. [第一张图片内容]
  2. [第二张图片内容]
  3. [第三张图片内容]
  ```
- 将上述结果合并为 `pic_processed` 字段
- 若该笔记无图片，`pic_processed` 填入 `0`

---

### F6 视频内容解析

**描述**：使用配置的 AI 大模型对笔记中的视频进行多模态解析，提取声音和画面信息。

**详细需求**：
- 对 `video_url_list` 中的视频，调用 AI 模型进行解析：
  - 分析视频**声音**（语音内容、背景音乐等）
  - 分析视频**画面**（场景、字幕、关键视觉内容等）
  - 将分析结果归纳为结构化文字，清晰呈现视频的主要内容
- 将解析结果存入 `video_processed` 字段
- 若该笔记无视频，`video_processed` 填入 `0`

---

### F7 内容总结生成

**描述**：使用配置的 AI 大模型，综合笔记的文字、图片解析和视频解析内容，生成一篇全面的内容总结。

**详细需求**：
- 输入：`text_original` + `pic_processed` + `video_processed`
- 调用 AI 模型进行综合分析，生成 `summary` 字段
- 总结应覆盖笔记的核心观点、关键信息和主要内容，不遗漏图片和视频中的重要信息
- 若以上三个输入字段均为 `0`（即无任何内容），`summary` 填入 `0`

---

### F8 数据回写 Google Sheets

**描述**：将每条笔记的全部处理结果写回原始 Google Sheets 表格对应行。

**详细需求**：
- 每条链接处理完成后，立即将结果写回对应行（实时写回，而非批量写回）
- 回写字段包括：`title`、`author`、`date`、`stars`、`text_original`、`pic_url_list`、`video_url_list`、`pic_processed`、`video_processed`、`summary`
- 若某字段值为空（如无图片、无视频），该字段**占位填入** `0`，不留空白
- 不修改 `link`、`auto`、`error` 字段（这三个字段由其他步骤管理）

---

### F9 完成状态检测

**描述**：处理完成后，系统检查每条链接的所有字段是否均已正确填写，并更新任务状态。

**详细需求**：
- 在工作流结束阶段，逐行检查初始任务列表中所有待处理链接的字段完整性
- 检查内容：`title`、`author`、`date`、`stars`、`text_original`、`pic_url_list`、`video_url_list`、`pic_processed`、`video_processed`、`summary` 字段是否均已填写（`0` 视为有效值）
- **处理成功**：所有字段均已填写 → 将 `auto` 字段更新为 `1`
- **处理失败**：存在未填写字段 → 在 `error` 字段中记录具体失败原因（如：`抓取失败：网络超时`、`图片解析失败：API 返回错误`）

---

## 4. 工作流时序

```
用户填写 Google Sheets 链接
        ↓
用户点击「开始运行」按钮
        ↓
F1: 读取 Google Sheets，筛选 auto 和 error 均为空的行
        ↓
对每一条链接（串行处理）：
  ├── F4: 抓取小红书笔记数据（携带 Cookie）
  ├── F5: 图片内容 AI 解析 → pic_processed
  ├── F6: 视频内容 AI 解析 → video_processed
  ├── F7: 综合内容 AI 总结 → summary
  └── F8: 将所有字段写回 Google Sheets 对应行
        ↓
F9: 检查所有链接处理状态，更新 auto / error 字段
        ↓
网页端展示运行完成摘要
```

---

## 5. 数据模型：Google Sheets 表格结构

| 列名 | 分类 | 数据类型 | 说明 |
|------|------|----------|------|
| `link` | 初始值 | 文本 | 用户填入的小红书笔记完整 URL |
| `title` | 抓取值 | 文本 | 笔记标题，无内容时为 `0` |
| `author` | 抓取值 | 文本 | 作者昵称，无内容时为 `0` |
| `date` | 抓取值 | 文本 | 发布时间，无内容时为 `0` |
| `stars` | 抓取值 | 数字/文本 | 收藏量，无内容时为 `0` |
| `text_original` | 抓取值 | 文本 | 笔记正文，无内容时为 `0` |
| `pic_url_list` | 抓取值 | 文本 | 图片 URL 列表（逗号分隔或 JSON），无图片时为 `0` |
| `video_url_list` | 抓取值 | 文本 | 视频 URL 列表，无视频时为 `0` |
| `pic_processed` | 解析值 | 文本 | 图片解析内容（编号排列），无图片时为 `0` |
| `video_processed` | 解析值 | 文本 | 视频解析内容，无视频时为 `0` |
| `summary` | 解析值 | 文本 | 笔记综合内容总结，无内容时为 `0` |
| `auto` | 状态值 | 数字 | 处理完成标记：`1`=完成，空=待处理或处理中 |
| `error` | 状态值 | 文本 | 错误原因描述，空=无错误 |

---

## 6. 非功能性需求

| 需求 | 描述 |
|------|------|
| **AI 模型可配置** | 不绑定特定 AI 厂商，用户可在网页端自由配置模型厂商、模型名称和 API Key |
| **容错处理** | 单条链接处理失败不中断整体工作流，失败项记录错误原因后继续处理下一条 |
| **实时回写** | 每条链接处理完成后立即写回 Google Sheets，避免中途中断导致数据丢失 |
| **配置持久化** | AI 模型配置和 Cookie 信息在本地持久化存储，重启后无需重新配置 |
| **API Key 安全** | API Key 在界面上以掩码形式展示，不明文显示 |
| **串行处理** | 链接依次顺序处理（非并发），降低 API 限流风险 |

---

## 7. 范围外（Out of Scope）

以下功能在本版本中**不包含**：

- 用户注册、登录、权限管理系统
- 多用户协作或团队共享功能
- 定时任务或自动化触发（本版本仅支持手动触发）
- 小红书账号管理（如多账号切换）
- 小红书内容发布或互动功能
- 除 Google Sheets 以外的数据源或输出格式支持
- 数据可视化或图表分析功能

---

## 8. 验收标准

| 编号 | 验收项 | 验收方式 |
|------|--------|----------|
| AC-1 | 点击「开始运行」后，系统正确读取 Google Sheets 中 `auto` 和 `error` 均为空的行 | 手动测试：在表格中准备 5 条链接（其中 2 条 auto=1），确认系统只处理剩余 3 条 |
| AC-2 | 每条链接的 7 个抓取字段均能正确写入 Google Sheets | 手动验证表格数据是否与小红书原始笔记内容一致 |
| AC-3 | 有图片的笔记生成的 `pic_processed` 内容按编号排列，能体现每张图片的内容 | 人工核查 AI 解析结果与实际图片是否对应 |
| AC-4 | 有视频的笔记生成的 `video_processed` 包含声音和画面的综合描述 | 人工对比视频内容与解析文字 |
| AC-5 | `summary` 字段覆盖文字、图片、视频三个维度的核心内容 | 人工审阅总结质量 |
| AC-6 | 无图片的笔记，`pic_processed` 字段填入 `0`，而非空白 | 验证含无图片笔记的行 |
| AC-7 | 成功处理的链接 `auto=1`，失败链接 `error` 字段有具体错误描述 | 手动构造一条无效链接，确认错误被记录 |
| AC-8 | 网页端实时显示处理进度和日志，每条链接的每个步骤有状态反馈 | 观察网页 UI 在运行过程中的更新 |
| AC-9 | AI 模型配置保存后重新打开页面仍然有效 | 关闭并重新打开网页，验证配置已持久化 |
| AC-10 | Cookie 状态面板能正确反映当前浏览器的小红书登录状态 | 登录/退出小红书账号，验证 Cookie 状态变化 |

---

*本文档基于 `prd_raw.md` 整理，版本 v1.0，日期 2026-02-24。*
